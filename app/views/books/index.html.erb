<h1>Books</h1>
<ul>
  <% @books.each do |book| %>
    <li>
      <%= link_to book.title, book_path(book) %> - <%= book.author %>
      <%= book.borrowed? ? '(Borrowed)' : '(Available)' %>
    </li>
  <% end %>
</ul>